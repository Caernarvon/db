language: java

jdk:
  - oraclejdk8
  
branches:
  only:
    - test
before_install:
  - git add -f gradle/wrapper/gradle-wrapper.jar
  - chmod +x gradle/wrapper/gradle-wrapper.jar
  - chmod +x gradlew

script:
  - ./gradlew build
